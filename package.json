{
  "name": "axion-hub-electron",
  "version": "1.0.0",
  "description": "Axion Hub — launcher (Electron + Vite + React + TS)",
  "author": "Your Name",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build:renderer": "vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "build": "npm run build:renderer && npm run build:electron && electron-builder",
    "start": "electron .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@types/node": "^20.0.0",
    "concurrently": "^8.0.0",
    "wait-on": "^7.0.0",
    "esbuild": "^0.19.0",
    "@vitejs/plugin-react": "^4.0.0"
  },
  "build": {
    "appId": "com.axion.hub",
    "productName": "Axion Hub",
    "files": [
      "dist/**",
      "electron/main.js",
      "electron/preload.js",
      "package.json"
    ],
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": ["nsis"],
      "icon": "build/icon.ico"
    }
  }
}